
Start date 02/10/2026

version 8.2
check the environment if the php add in the path 
download xampp

run 
composer global require laravel/installer

### Create project
laravel new app-name

#### Connect to Github
create a repo in github 

```
git init
git add . git commit -m "Initial commit"
git branch
git branch -m main
git remote -v
git remote set-url origin https://github.com/YOUR_USERNAME/Laravel-Project.git
git remote add origin https://github.com/YOUR_USERNAME/Laravel-`Project.git
git pull origin main --allow-unrelated-histories
```

