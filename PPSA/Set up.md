 clone the ppsa_registry
	download python13
		python -m venv .venv
		Update interpreter in the VSCode, use the new .venv
		Reinstall packages

create .flaskenv
port = 8002

	1. Open the **Command Palette** by pressing `Ctrl+Shift+P` (Windows/Linux) or `Cmd+Shift+P` (macOS).
   2. Type `Python: Select Interpreter` into the search bar and select it.
3. VS Code will display a list of discovered Python environments. Look for an option that points to the Python executable inside your `venv` folder (e.g., `./venv/Scripts/python.exe` on Windows or `./venv/bin/python` on macOS/Linux).

		pip install -r requirements.txt

un install 
```python
pip uninstall python-magic python-magic-bina
```


then install 
```python
pip install python-magic-bin
```


install the filename check jed message telegram
	msys2-x86_64-20241208.exe

after 
paste this command
	`pacman -S mingw-w64-x86_64-gtk3 mingw-w64-x86_64-pango mingw-w64-x86_64-cairo mingw-w64-x86_64-gobject-introspection`


create new .environment copy the enviroment.sample  check the message in telegram

create file flaskenv paste the code inside to change the port 

	FLASK_APP=app
	FLASK_DEBUG=1
	FLASK_RUN_PORT=8002

then flask run


